<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StartLocal - Login Empresa</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="d-flex flex-column min-vh-100">

    <%- include('partials/navbar') %>

    <main class="flex-grow-1 d-flex justify-content-center align-items-center py-5">
        <div class="card p-4 shadow form-login">
            <div class="text-center mb-4">
                <i class="bi bi-building-fill display-4 text-warning"></i>
                <h3 class="mt-2">Login — Empresa</h3>
            </div>

            <% if (erro) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle me-2"></i><%= erro %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>

            <form action="/login-empresa" method="POST">
                <div class="mb-3">
                    <label class="form-label fw-semibold">E-mail Corporativo</label>
                    <input type="email" name="email" class="form-control" required placeholder="contato@empresa.com"/>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Senha</label>
                    <input type="password" name="senha" class="form-control" required/>
                </div>
                <div class="mb-3 text-center">
                    <a href="#" class="text-muted small">Esqueci minha senha</a>
                </div>
                <div class="mb-3 text-center">
                    <p class="mb-0">Ainda não tem conta? <a href="/cadastro-empresa">Cadastre sua empresa</a></p>
                </div>
                <button type="submit" class="btn btn-warning w-100 fw-bold text-dark">
                    <i class="bi bi-box-arrow-in-right me-2"></i>Entrar como Empresa
                </button>
            </form>

            <hr class="my-3">
            <p class="text-center mb-0">
                <small class="text-muted">É candidato? </small>
                <a href="/login-candidato" class="small">Acesse aqui</a>
            </p>
        </div>
    </main>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
